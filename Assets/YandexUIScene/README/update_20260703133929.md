# 📝 更新记录: 二维码连接成功后渐显动画时长调整 (2026-07-03 13:39:29)

本次更新将网络连接成功后二维码的渐显动画（DOFade In）时长调整为 1.5 秒。

---

## 1. 核心更新与逻辑实现

### ✨ 1. 二维码淡入效果（DOFade In 1.5s）
* **淡入时间延长**：在 `YandexLeaderboardManager.cs` 的连接检测成功逻辑中，将原本瞬间显示的二维码或立刻置为不透明（alpha = 1）修改为在 **1.5 秒** 内平滑淡入。
* **安全性保障**：自动检查目标 RawImage 是否挂载 `CanvasGroup` 组件，若没有则动态添加，从而支持 DOTween 对 Alpha 的控制。

---

## 2. 涉及文件

* [YandexLeaderboardManager.cs](file:///d:/Projects_Every/unity%20projects/Yandex_UI_FLOW/Assets/YandexUIScene/CommandScripts/YandexLeaderboardManager.cs) (修改：设置 `targetRawImage` 连接时的 Alpha 变化逻辑为 1.5s 渐显)
